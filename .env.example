# ─── PostgreSQL (Docker) ──────────────────────────────────────────────────────
# Used by docker-compose.yml to configure the Postgres container
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=movie_app

# ─── Backend ──────────────────────────────────────────────────────────────────
# Prisma connection string — must match the Docker Postgres creds above
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/movie_app

# Auth0 — JWT validation (backend)
# AUTH0_DOMAIN: your tenant, e.g. your-tenant.auth0.com
# AUTH0_AUDIENCE: the API identifier you set in Auth0, e.g. https://movie-app-api
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=https://movie-app-api

# TMDB — read access token (Bearer token, not the v3 API key)
# Get it from: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your-tmdb-read-access-token

# Sentry — error tracking
# Get it from: https://sentry.io → your project → Settings → Client Keys (DSN)
SENTRY_DSN=https://your-key@sentry.io/your-project-id

# Server config
BACKEND_PORT=4000
FRONTEND_URL=http://localhost:3000
